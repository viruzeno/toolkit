# https://splunk.github.io/splunk-operator/Ingress.html#Configuring-Ingress-Using-NGINX
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: splunk-single-standalone-ingress
  namespace: splunk-operator
  annotations:
    # use the shared ingress-nginx
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/default-backend: splunk-single-standalone-service
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
spec:
  ingressClassName: nginx
  rules:
  - host: web1.server.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: splunk-search-standalone-service
            port:
              number: 8000
  - host: web2.server.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: splunk-cm-cluster-manager-service
            port:
              number: 8000
  - host: web3.server.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: splunk-monitor-monitoring-console-service
            port:
              number: 8000

